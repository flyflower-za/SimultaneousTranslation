# 会议纪要开发 TODO

## 已完成

- [x] 保存原文与译文的定稿字幕、时间戳和会议记录；短暂断线仍归入同一场。
- [x] 点击停止或房间超时回收后结束会议，进程重启时补齐未收尾会议。
- [x] 使用独立、可配置的文本模型生成中文纪要；原文为事实依据，译文作辅助；长会议分段处理。
- [x] 管理后台查看会议和字幕、重新生成、编辑草稿、发布、撤回、撤销分享、下载 Markdown 和删除会议。
- [x] 查看端沿分享链接查看已发布纪要；生成或审核期间显示状态。
- [x] 从 Git 索引移除运行时 SQLite 数据文件，避免新增会议内容继续进入版本控制。

## 上线前

- [ ] 在管理后台“纪要模型”页或 `config/config.json` 配置模型地址、名称与密钥，填写实际 Token 单价。
- [ ] 使用真实模型做一场中英双语会议的端到端验收，检查纪要事实、时序与专有名词。
- [ ] 确定字幕和纪要的保留期限与删除策略；历史 Git 提交中的数据库需单独评估处置。

## 后续增强

- [x] 给控制端用户增加“我的会议”历史列表和纪要查看入口，访问码之间隔离。
- [x] 增加 PDF 下载、人工修订历史与发布版本记录，可预览并恢复旧版本。
- [x] 在管理后台增加纪要模型配置页，API Key 只写入服务器数据库、不回传页面。
- [x] 使用媒体时间与字幕顺序配对原文和译文；长会议逐层合并摘要。
- [x] 记录每次模型调用的 Token 与按配置单价计算的成本；失败任务持久化并指数退避重试。
